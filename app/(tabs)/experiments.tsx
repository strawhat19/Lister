import { Text, View } from '@/components/Themed';

export default function Experiments() {
  return (
    <View style={{ flex: 1, width: `100%`, backgroundColor: `black`, justifyContent: `center`, alignItems: `center` }}>
      <Text style={{ fontWeight: `bold`, fontSize: 20 }}>
        Experiments
      </Text>
    </View>
  )
}